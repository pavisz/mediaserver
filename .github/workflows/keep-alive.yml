name: Overdrive Keep-Alive
on:
  schedule:
    - cron: "*/15 * * * *" 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Server
        run: curl -I https://pavisz-mediaserver-backend.hf.space
